@model IEnumerable<DASH_BOOKING.Models.EventModel>

@{
    ViewBag.Title = "Event List";
    Layout = "~/Views/Shared/Panel_Layout.cshtml";
}

<section class="ftco-section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6 text-center mb-5">
                <h2 class="heading-section">Event List</h2>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <div class="table-wrap">
                    <table class="table table-responsive-xl table-bordered table-striped">
                        <thead class="thead-dark">
                            <tr>
                                <th>Image</th> <!-- Image moved to the first column -->
                                <th>Event Name</th>
                                <th>Description</th>
                                <th>Event Date</th>
                                <th>Event Time</th>
                                <th>Location</th>
                                <th>Organizer Email</th>
                                <th>Organizer Phone</th>
                                <th>Action</th> <!-- Action column for Edit and Delete buttons -->
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model != null && Model.Any())
                            {
                                var count = 0;
                                foreach (var eventModel in Model)
                                {
                                    <tr class="@(count++ % 2 == 0 ? "even-row" : "odd-row")">
                                        <td>
                                            @if (eventModel.Images != null && eventModel.Images.Any())
                                            {
                                                var firstImage = eventModel.Images.FirstOrDefault();
                                                <img src="@Url.Content(firstImage.Image)" alt="Event Image" style="max-width: 100px;" />
                                            }
                                            else
                                            {
                                                <span>No Image</span>
                                            }
                                        </td>
                                        <td>@eventModel.EventName</td>
                                        <td>@eventModel.Description</td>
                                        <td>@eventModel.EventDate.ToString("MM/dd/yyyy")</td>
                                        <td>@eventModel.EventTime.ToString("hh:mm tt")</td>
                                        <td>@eventModel.Location</td>
                                        <td>@eventModel.OrganizerEmail</td>
                                        <td>@eventModel.OrganizerPhone</td>
                                        <td>
                                            <div class="btn-group" role="group" aria-label="Event Actions">
                                                <a href="@Url.Action("Edit", "Accounts", new { id = eventModel.Id })" class="btn btn-primary">Edit</a>
                                                <a href="@Url.Action("Delete", "Accounts", new { id = eventModel.Id })" class="btn btn-danger">Delete</a>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="9">No events found.</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                <!-- Edit Event Modal -->
                <div class="modal fade" id="editEventModal" tabindex="-1" role="dialog" aria-labelledby="editEventModalLabel" aria-hidden="true">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title" id="editEventModalLabel">Edit Event</h5>
                                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                    <span aria-hidden="true">&times;</span>
                                </button>
                            </div>
                            <div class="modal-body">
                                <!-- Add your form fields here for editing event details -->
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                                <button type="button" class="btn btn-primary">Save Changes</button>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Delete Confirmation Modal -->
                <div class="modal fade" id="deleteConfirmationModal" tabindex="-1" role="dialog" aria-labelledby="deleteConfirmationModalLabel" aria-hidden="true">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title" id="deleteConfirmationModalLabel">Confirm Deletion</h5>
                                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                    <span aria-hidden="true">&times;</span>
                                </button>
                            </div>
                            <div class="modal-body">
                                Are you sure you want to delete this event?
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                                <button type="button" class="btn btn-danger">Delete Event</button>
                            </div>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
</section>

<style>
    .table-wrap {
        overflow-x: auto;
        margin-bottom: 20px;
    }

    .table {
        border-collapse: collapse;
        width: 100%;
    }

        .table th,
        .table td {
            padding: 10px;
            text-align: center;
            vertical-align: middle;
        }

        .table th {
            font-weight: 600;
            background-color: #343a40;
            color: #fff;
        }

        .table td {
            font-weight: 400;
        }

    .table-striped tbody tr:nth-of-type(odd) {
        background-color: rgba(0, 0, 0, .05);
    }

    .table-hover tbody tr:hover {
        background-color: rgba(0, 0, 0, .075);
    }

    .even-row {
        background-color: #f5f5f5; /* Light gray background for even rows */
    }

    .odd-row {
        background-color: #fff; /* White background for odd rows */
    }
</style>


<script>
    $(document).ready(function () {
        // Function to show the Edit Event modal
        $('#editEventModal').on('show.bs.modal', function (e) {
            // Retrieve the event ID and fetch event details via AJAX if needed
            var eventId = $(e.relatedTarget).data('event-id');
            // Example AJAX call
            // $.ajax({
            //     url: '/Accounts/GetEventDetails',
            //     type: 'GET',
            //     data: { id: eventId },
            //     success: function (response) {
            //         // Populate modal with event details
            //     },
            //     error: function (xhr, status, error) {
            //         // Handle error
            //     }
            // });
        });

        // Function to show the Delete Confirmation modal
        $('#deleteConfirmationModal').on('show.bs.modal', function (e) {
            // Retrieve the event ID and set it in the modal for deletion
            var eventId = $(e.relatedTarget).data('event-id');
            $('#deleteConfirmationModal').data('event-id', eventId);
        });

        // Handle Save Changes button click in Edit Event modal
        $('#editEventModal').on('click', '.btn-primary', function () {
            // Perform form validation and update event details via AJAX
            // Example AJAX call
            // $.ajax({
            //     url: '/Accounts/EditEvent',
            //     type: 'POST',
            //     data: $('#editEventForm').serialize(), // Assuming form ID is editEventForm
            //     success: function (response) {
            //         // Handle success (e.g., close modal, refresh page, etc.)
            //     },
            //     error: function (xhr, status, error) {
            //         // Handle error
            //     }
            // });
        });

        // Handle Delete Event button click in Delete Confirmation modal
        $('#deleteConfirmationModal').on('click', '.btn-danger', function () {
            // Retrieve the event ID and perform deletion via AJAX
            var eventId = $('#deleteConfirmationModal').data('event-id');
            // Example AJAX call
            // $.ajax({
            //     url: '/Accounts/DeleteEvent',
            //     type: 'POST',
            //     data: { id: eventId },
            //     success: function (response) {
            //         // Handle success (e.g., close modal, refresh page, etc.)
            //     },
            //     error: function (xhr, status, error) {
            //         // Handle error
            //     }
            // });
        });
    });

</script>